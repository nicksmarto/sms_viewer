# Example environment file.
# Copy this to a file named ".env" (which is gitignored) and fill in real values.
# NEVER put real secrets or personal data in THIS example file — it is committed.

# Your own phone number, digits only (e.g. 5551234567).
# Used to identify "you" when detecting participants in group conversations.
MY_PHONE_NUMBER=
